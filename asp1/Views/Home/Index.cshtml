@*@using asp1.ViewModels;
@model HomeVW;*@

@{
    ViewData["Title"] = "Index";
    Layout = "_Layout";
}



<p>@ViewData["Name"]</p>
<ul>
    @foreach (var item in ViewBag.Surname)
        {
            <li>
                @item.Surname
            </li>
        }
</ul>
<p>@TempData["25"]</p>

@*<div class="container">
    <div class="row">
        <ul class="list-group">
            @foreach (var item in Model.Students)

            {

                <li class="list-group-item">@item.Id @item.Name @item.Surname </li>

            }
        </ul>

        <ul class="list-group">
            @foreach (var item in Model.Groups)
            {

                <li class="list-group-item">@item.Id @item.Name  </li>

            }
        </ul>
    </div>
</div>*@

